# 文档搜索索引 v1.0 使用说明

## 一、快速开始（生成 exe）

### 第一步：安装 Python
如果你的电脑还没有 Python，请先下载安装：
- 下载地址：https://www.python.org/downloads/
- 版本要求：Python 3.8 或更高版本
- **安装时必须勾选 "Add Python to PATH"**

### 第二步：一键打包
双击运行 `一键打包exe.bat`，等待 3~8 分钟，自动生成 exe 文件。

打包完成后，exe 文件位于 `dist\文档搜索索引_v1.0.exe`。

---

## 二、软件使用方法

### 新建索引（第一次使用）
1. 双击打开 `文档搜索索引_v1.0.exe`
2. 点击菜单栏 **"索引"** → **"新建索引..."**
3. 选择要索引的文件夹（比如整个 D 盘）
4. 选择索引文件保存位置（会生成一个 `.db` 文件）
5. 勾选要索引的文件格式（默认已勾选 Word/Excel/PPT）
6. 点击确定，等待索引完成

> 索引速度参考：1000 个文档大约需要 1~3 分钟（取决于文件大小）

### 搜索文档
1. 在顶部搜索框输入关键词
2. 按回车或点击"搜索"按钮
3. 左侧列表显示匹配的文档
4. 右侧显示关键词命中的上下文摘要

### 打开文件
- **单击**搜索结果：右侧显示摘要和文件信息
- **双击**搜索结果：
  - 如果硬盘在线 → 直接用系统默认程序打开文件
  - 如果硬盘不在线 → 弹出离线预览窗口

### 离线预览（硬盘不在时）
双击不在线的文件，会弹出预览窗口：
- **"关键词命中"标签**：显示所有包含关键词的段落
- **"查看全文"标签**：显示文件的完整文字内容（索引时提取的）

### 加载已有索引
1. 点击菜单栏 **"索引"** → **"加载索引..."**
2. 选择之前保存的 `.db` 文件
3. 即可搜索，无需重新建立索引

### 更新索引（新增了文件后）
1. 点击菜单栏 **"索引"** → **"更新索引"**（或按 F5）
2. 软件会自动扫描新增和修改的文件，跳过未变化的文件
3. 速度很快，只处理变化的部分

---

## 三、关于 PDF 支持

| PDF 类型 | 说明 | 是否支持 |
|---------|------|---------|
| 可复制文字的 PDF | 正常的 PDF，可以选中文字 | ✅ 支持 |
| 扫描版 PDF | 扫描的图片，不能选中文字 | 需要开启 OCR |

**开启 OCR 的方法：**
1. 新建索引时，勾选"启用 OCR 识别扫描版 PDF"
2. 需要额外安装 Tesseract OCR 软件
3. 注意：OCR 速度较慢，建议仅在需要时开启

---

## 四、索引文件说明

- 索引文件是一个 `.db` 文件（SQLite 数据库）
- 可以复制到任意位置，不影响使用
- 文件大小参考：1000 个文档约 10~50 MB（取决于文档内容量）
- 可以随时备份，防止重新索引

---

## 五、常见问题

**Q：搜索没有结果？**
A：确认已建立索引，且搜索的关键词在文档中确实存在。

**Q：某些文件没有被索引？**
A：检查"新建索引"时是否勾选了对应的文件格式。

**Q：.doc 和 .ppt 格式提取内容为空？**
A：老格式（.doc/.ppt）需要系统安装 LibreOffice 才能提取内容。
建议优先使用 .docx/.pptx 格式。

**Q：打包时杀毒软件报警？**
A：这是误报，PyInstaller 打包的程序经常被杀毒软件误判。
可以暂时关闭杀毒软件后再打包，或者将 dist 目录加入白名单。

---

## 六、技术信息

| 项目 | 说明 |
|------|------|
| 开发语言 | Python 3.10 |
| 界面框架 | PyQt5 |
| 索引引擎 | SQLite FTS5 |
| 中文分词 | jieba |
| 打包工具 | PyInstaller |
| 系统要求 | Windows 10 64位 |
